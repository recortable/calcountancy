<% div_for( tag, :style => "background-color: #{tag.color}" ) do %>
  <%= icon_link_to('delete_small', t('.delete_tag'), user_account_tag_path(tag.account, tag), :method => :delete, :confirm => "¿Seguro que quieres borrar la etiqueta '#{tag.name}' de la cuenta '#{tag.account.name}'?") if tag.size == 0 %>
  <%= icon_link_to 'edit_small', t('.edit_tag'), edit_user_account_tag_path(tag.account, tag) %>
  <%= link_to "#{tag.name} (#{tag.size})", user_account_tag_path(tag.account, tag) %>
<% end %>