
var content = '<%= escape_javascript render(@movement.taggings) %>';
$("#tagging_movement_<%= @movement.id %>").html(content);
$.cy.flash("<%= t('.success', :tag => @tag.name, :movement => @movement.concept) %>");

